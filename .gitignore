# Secrets / credentials — NEVER upload
.env
*.plist

# Dev/helper scripts kept out of the repo (main pipeline scripts are tracked;
# pipeline secrets live in GitHub Actions secrets, never in code)
# Bare names anchored to / so they only ignore root-level copies, not scripts/
/zoom_fetcher.py
/meetings_config.py
scripts/test_zoom_token.py
scripts/notifications.py

# Root-level duplicate scripts (not part of repo; anchored to / so the
# tracked copies under scripts/ are not affected)
/debug_github.py
/download_transcripts.py
/github_uploader.py
/pmissues_monitor.py
/test_upload.py

# Python / venv
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# OS
**/.DS_Store

# Runtime artifacts
run.sh
logs/
downloads/

# Local-only outputs (never upload)
acd_participants.md
scripts/export_acd_participants.py
EIP-7907_*.md
context.html
